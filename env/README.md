# Agent environments

Create one `.env` file per agent from its repository `.env.example`. Use a dedicated CROO SDK key, agent ID, service ID, AA wallet, and PostgreSQL database/schema for each worker. Never reuse identities or commit these files.
